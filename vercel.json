{
  "version": 2,
  "builds": [
    { "src": "/front", "use": "@vercel/angular" },
    { "src": "/api", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/front/(.*)", "dest": "/front/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
